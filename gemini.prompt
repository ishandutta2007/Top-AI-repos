model: googleai/gemini-1.5-pro
input:
  schema:
    text: in this readme.md you will find a markdown table and there are some github repo links following it. I want you to add new rows below the existing rows of this table with those links in repo column and their appropiate details in other columns.
---

model: googleai/gemini-1.5-pro
input:
  schema:
    text: For the table in README.md, Sort the rows by Repo_Stars (descending order). Note the stars are not given there as numbers but as a badge(image)
---

model: googleai/gemini-1.5-pro
input:
  schema:
    text: in readme.me, please fix the rowid for the rows in the table
---

model: googleai/gemini-1.5-pro
input:
  schema:
    text: in readme.md, Can you add a column for initiation year of these projects mentioned in the table, you can get that information by looking up the date on the oldest commit on github.
---

model: googleai/gemini-1.5-pro
input:
  schema:
    text: In the markdown table in readme.md file, add the "created year" column and fetch the creation year info for the first row ie this repo https://github.com/Significant-Gravitas/AutoGPT and populate that row's value for that new column.
---
model: googleai/gemini-1.5-pro
input:
  schema:
    text: In the markdown table in readme.md file, add the value for "created year" column for the 2nd row. You will get the repo link from 2nd column value. And information about created year can be fetched by accessing the oldest commit by command `git log --reverse`, to run that command you might have to clone the repo temporarily as well.
---
model: googleai/gemini-1.5-pro
input:
  schema:
    text: great, can you do the same for 3rd row
---
model: googleai/gemini-1.5-pro
input:
  schema:
    text: great, can you do the same for 4th row onwards one by one. after completion of each row make sure you commit the code changes. And after completion of each row prompt me if I want to preceed for the next row.
---
